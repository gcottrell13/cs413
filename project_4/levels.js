var levels = [];
var num_levels = 8;

levels.solids = [1, 2, 3, 4, 5, 6, 7, 11, 13, 14, 18, 19, 20, 21, 22];
levels.end = [8];
levels.tileset = 'tileset.png';

levels[1] = {
	map: 'map1_json',
	tileset: levels.tileset,
	solid_blocks: levels.solids,
	end_blocks: levels.end
};

levels[2] = {
	map: 'map2_json',
	tileset: levels.tileset,
	solid_blocks: levels.solids,
	end_blocks: levels.end
};

levels[3] = {
	map: 'map3_json',
	tileset: levels.tileset,
	solid_blocks: levels.solids,
	end_blocks: levels.end
};

levels[4] = {
	map: 'map4_json',
	tileset: levels.tileset,
	solid_blocks: levels.solids,
	end_blocks: levels.end
};

levels[5] = {
	map: 'map5_json',
	tileset: levels.tileset,
	solid_blocks: levels.solids,
	end_blocks: levels.end
};

levels[6] = {
	map: 'map6_json',
	tileset: levels.tileset,
	solid_blocks: levels.solids,
	end_blocks: levels.end
};

levels[7] = {
	map: 'map7_json',
	tileset: levels.tileset,
	solid_blocks: levels.solids,
	end_blocks: levels.end
};

levels[8] = {
	map: 'map8_json',
	tileset: levels.tileset,
	solid_blocks: levels.solids,
	end_blocks: levels.end
};


